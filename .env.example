# ============================================
# CES TestSuiteAgent Environment Configuration
# ============================================
# Copy this file to .env and fill in your own values.
# ⚠️ NEVER commit .env with real credentials!

# --------------------------------------------
# Google Cloud Configuration (REQUIRED)
# --------------------------------------------
# Your GCP project ID where Dialogflow CX agent is deployed
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id

# Path to your service account JSON key file
# Download from: GCP Console > IAM > Service Accounts > Keys
GOOGLE_APPLICATION_CREDENTIALS=./your-service-account-key.json

# --------------------------------------------
# Dialogflow CX Agent Configuration (REQUIRED)
# --------------------------------------------
# Agent location (e.g., global, us-central1, europe-west1)
DIALOGFLOW_LOCATION=global

# Your Dialogflow CX Agent ID (UUID format)
# Find in: Dialogflow CX Console > Agent Settings
DIALOGFLOW_AGENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# --------------------------------------------
# Gemini AI Configuration (REQUIRED for AI Analysis)
# --------------------------------------------
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Model options: gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash
GEMINI_MODEL=gemini-1.5-flash

# --------------------------------------------
# Backend Server Configuration
# --------------------------------------------
PORT=3001
NODE_ENV=development

# --------------------------------------------
# Redis Configuration (OPTIONAL - for bulk test queue)
# --------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# SQLite database path for local development
DATABASE_URL=./data/database.sqlite

# --------------------------------------------
# Frontend Configuration
# --------------------------------------------
# Frontend URL for CORS (update for production)
FRONTEND_URL=http://localhost:5173

# --------------------------------------------
# Test Execution Settings
# --------------------------------------------
# Maximum concurrent test executions
MAX_CONCURRENCY=5

# Timeout for individual tests (milliseconds)
TEST_TIMEOUT_MS=30000

# Number of retry attempts for failed tests
RETRY_ATTEMPTS=2
